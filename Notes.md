1. Designer - Add a menu function to take a set of selected open path shapes (unclosed polylines, lines etc.) and convert them into a single path. - Done
2. Designer - add a closed property to polyline( path) objects to close the start to the end if they are not colocated. 
3. Designer - Add the ability to convert a polyline (path) object to a rectangle based on its bounding box, support multiple selected shapes. this provides a simple way of converting polyline slots into a rectangle that can be set to a slot. - Done
4. Designer - There is a bug in changing the dimension properties of a multiselect set - Fixed. 
5. UI - setup menu to use a mono font for menus, put the menubar inside a rectangle with the correct font family. 
6. UI - work on switching themes. 
7. UI - The bottom of the progress widget is being clipped, the tails on the "g" s etc are cut.
8. UI - test all scenarios with undo/redo in designer and make sure they all work. 
9. Designer - add the ability to rotate shapes. rotate in world coordinates. Done however we need to look at compound shapes and the center of rotation. 
10. The Camtools can have multiple "instances" of the tool open, restrict this to a single instance. 
